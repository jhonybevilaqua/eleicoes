[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gctse"
version = "1.0.0"
description = "Apuracao eleitoral do TSE: tarjas para o GC (gctse) e telas cheias para o telao"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["requests>=2.31", "PyYAML>=6.0"]

[project.scripts]
gctse = "gctse.cli:main"
telao = "telao.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
